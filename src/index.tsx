import React from 'react';
import ReactDOM from 'react-dom/client';
import IQventionWebsite from './App/App.tsx';

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);
root.render(
  <React.StrictMode>
    <IQventionWebsite />
  </React.StrictMode>
);
